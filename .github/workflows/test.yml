name: CI

# Runs on pushes and pull requests to main
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Tests will be added here as pipeline services are implemented.
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: No tests yet
        run: echo "CI pipeline placeholder — tests will be added per service."
