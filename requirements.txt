# Core pipeline
pymupdf>=1.24.0          # Stage 2: PDF text extraction
rapidfuzz>=3.0.0         # Stage 3: fuzzy heading matching
lxml>=5.0.0              # Stage 6: XML generation + XSD validation

# Stage 5c post-processor
python-dateutil>=2.9.0   # date string parsing → ISO 8601
httpx>=0.27.0            # GLEIF API calls

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Dev tooling
ruff>=0.4.0                # linter + formatter (replaces black + isort + flake8)

# Future / Phase 2
# fastapi
# uvicorn[standard]
# python-multipart
# docling
# redis
